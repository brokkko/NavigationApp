
// Not sure about this one, anyway at some point we will have backend, so it will be replaced

export const serverURL = "http://localhost:3000"
export const remoteURL = "http://localhost:8080"

export const locationsURL = `${remoteURL}/api/locations`
// export const locationsURL = `${serverURL}/api/locations`
